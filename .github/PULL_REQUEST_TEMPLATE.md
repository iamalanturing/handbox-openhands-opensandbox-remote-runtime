## Summary

<!-- What does this change do, and why? -->

## Test plan

<!-- How was this verified? go build/vet/test, manual testing against a
     live OpenSandbox instance, etc. -->

## Testing this PR locally

```
git fetch origin pull/<PR_NUMBER>/head:pr-<PR_NUMBER> && git checkout pr-<PR_NUMBER>
```

<!-- Replace <PR_NUMBER> with this PR's number once it's opened. -->

## AI provenance

<!-- AIPM disclosure (https://aipmq.org) — how much AI was involved in this PR.

     Humans: generate a fresh disclosure with the web generator at
     https://aipmq.org/current (always resolves to the latest AIPM spec
     version) and paste the resulting URL below.

     AI agents: use the pinned pattern below as-is — fill in role (this
     repo's default is prompted+reviewed), date (YYYY-MM-DD), and a short
     ctx description. Omit model if you'd rather not name a specific tool.
     Do not fetch aipmq.org/current to pick a version; only switch to a
     newer spec version once a human has updated this pinned pattern. -->

https://aipmq.org/1.1/aipm/#v=1.1&role=<ROLE>&date=<DATE>&ctx=<BRIEF_DESCRIPTION>&show=1
